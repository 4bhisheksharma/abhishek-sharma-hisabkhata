import 'package:flutter/material.dart';

class HybridSwitchPage extends StatelessWidget {
  const HybridSwitchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('HybridSwitch')),
      body: const Center(child: Text('HybridSwitch Page')),
    );
  }
}
